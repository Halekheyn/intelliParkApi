Qué sigue ahora

Ahora seguimos con el Bloque 3A — Registro de usuarios.

No vamos a hacer todo auth todavía. Primero haremos solo:

POST /api/auth/register

Objetivo: registrar un usuario en la tabla users, cifrando la contraseña con bcrypt.

Bloque 3A — Registro de usuarios
A. Objetivo del módulo

Crear el primer endpoint real del backend:

POST /api/auth/register

Este endpoint debe:

Recibir nombre, apellido, correo, contraseña y rol.
Validar datos básicos.
Verificar que el correo no exista.
Cifrar la contraseña con bcrypt.
Guardar el usuario en PostgreSQL.
Responder el usuario creado sin mostrar la contraseña.
B. Archivos que vamos a crear o modificar
src/
  app.js
  routes/
    auth.routes.js
  controllers/
    auth.controller.js
  services/
    auth.service.js
  database/
    users.db.js
  utils/
    password.util.js

C. Responsabilidad de cada archivo
| Archivo              | Responsabilidad                                  |
| -------------------- | ------------------------------------------------ |
| `auth.routes.js`     | Define la ruta `/register`.                      |
| `auth.controller.js` | Recibe la petición HTTP y responde al cliente.   |
| `auth.service.js`    | Maneja la lógica de negocio del registro.        |
| `users.db.js`        | Ejecuta consultas SQL relacionadas con usuarios. |
| `password.util.js`   | Cifra contraseñas usando `bcrypt`.               |
| `app.js`             | Conecta las rutas de autenticación con Express.  |


Postman
  ↓
Route
  ↓
Controller
  ↓
Service
  ↓
Database
  ↓
PostgreSQL